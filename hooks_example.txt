sources:
    styles:
        style_main
    body:
        body_main
        body_menu
        body_settings
        body_join
        body_chat
    script:
        script_main

hooks:
    ui:
        ui.log:pre
        ui.log_file:pre
        ui.log_file:before
        ui.minimize_to_tray:show_window
        ui.minimize_to_tray:quit_app
        ui.minimize_to_tray:run_tray
        ui.minimize_to_tray:pre
        ui.send_file_from_ui
        ui.close_chat
        ui.set_setting
        ui.add_plugin
        ui.on_loaded
        ui.init
        api:
            ui.api.send_message
            ui.api.send_file
            ui.api.send_any
            ui.api.create_chat
            ui.api.join_chat
            ui.api.exit_chat
            ui.api.open_shared_folder
            ui.api.apply_settings
            ui.api.reconnect
            ui.api.new
            ui.api.button_pressed
    server:
        server.client_processing_start
        server.client_processing:pre
        server.client_processing:after_msg_work
        server.client_processing:command_proceed
        server.on_client_disconnect:pre
        server.command_proceed
        server.plugin_update
    client:
        client.client_receiving_start
        client.client_receiving_processing:pre
        client.client_receiving_processing:after_msg_work
        client.client_receiving_processing:end
        client.client_disconnect:pre
        client.client_disconnect:before_clear
        client.client_disconnect:end
        client.illegal_execution
        client.plugin_update
    crh:
        crh.update_received_messages:end
    chat:
        chat.send